fcvgenetica.theme Installation
------------------------------

To install fcvgenetica.theme using zc.buildout and the plone.recipe.zope2instance
recipe to manage your project, you can do this:

* Add ``fcvgenetica.theme`` to the list of eggs to install, e.g.:

    [buildout]
    ...
    eggs =
        ...
        fcvgenetica.theme
       
* Re-run buildout, e.g. with:

    $ ./bin/buildout
